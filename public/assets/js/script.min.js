document.querySelectorAll(".datepicker").forEach((function(e) {
    new Pikaday({
        field: e
    })
}));

$(function() {
    $('body').on('click', '[data-link-to]', function() {
        $url = $(this).attr('data-link-to');
        window.location.href = $url;
    });
});
